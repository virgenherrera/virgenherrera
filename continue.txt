CONTINUE — Lunes

Estado actual:
- Branch: feat/monorepo-scaffold (todo pusheado, commit dff74ec)
- Monorepo: libs/ (profile, secrets) + apps/ (readme, ghpages)
- Todo pasa: eslint, prettier, tsc, playwright e2e

Pendiente inmediato:
- Crear PR feat/monorepo-scaffold → master

apps/ghpages — siguiente iteracion:
1. Diseño bonito (Tailwind, mobile first layout profesional, dark mode)
2. Inyeccion de data desde libs/profile segun rutas (getProfile() → componentes)
3. ngrx/signals para state management reactivo
4. Routing: /, /experience, /projects, /contact
5. Three.js para hero section o efectos visuales (opcional, soñar no cuesta nada)
6. Generar PDFs al vuelo (resume descargable desde la GH Page)
7. SEO: meta tags, og:image, structured data (agregar tests de Playwright para esto)

apps/readme — pendiente:
- Integration tests (correr el app y verificar output)

Ideas futuras:
- apps/linkedin — API pusher (actualizar perfil de LinkedIn desde profile.json)
- CI/CD con GitHub Actions (build + deploy GH Pages automatico)
- npm publish de libs/ para reutilizar en otros repos
