echo '
INFO: This project has a pre-push hook that runs e2e tests and build verification.
If necessary, you can disable this with the "--no-verify" flag during the push.
Example:
    "git push --no-verify"
'

pnpm run build && \
pnpm run test:e2e || \
{
  echo "ERROR: Pre-push checks failed. Please address issues before pushing."
  exit 1
}
